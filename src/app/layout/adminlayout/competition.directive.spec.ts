import { CompetitionDirective } from './competition.directive';

describe('CompetitionDirective', () => {
  it('should create an instance', () => {
    const directive = new CompetitionDirective();
    expect(directive).toBeTruthy();
  });
});
